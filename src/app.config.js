(function() { /* IIFE */
  angular
    .module("landmarkRegApp")
    .config(configure);

  function configure() {
    /* Initialization code that will run before the Angular app starts */
  }
})(); /* IIFE */
